import { Allura, Inter, Lato } from "next/font/google";

export const allura = Allura({ weight: "400", subsets: ["latin"] });
export const inter = Inter({ weight: "300", subsets: ["latin"] });
export const lato = Lato({ weight: "400", subsets: ["latin"] });
